<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guzellikappp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application

        android:label="Cilt bakımı ve Doğal maskeler"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/s"
        android:largeHeap="true" >
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Bakim"></activity>
        <activity android:name=".icerik"></activity>
        <activity android:name=".icerikEl"></activity>
        <activity android:name=".icerikAyak"></activity>
        <activity android:name=".icerikSac"></activity>
    </application>

</manifest>